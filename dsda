$(document).ready(function () {
                                                                                           
// Open Weather API
const apikey = "d6c3151200124f6b3dfdd52227d4e82c";

var username = "nbmcsa";
var password =  "goosDoup23@&%";

var search_term = "nbmcsa"

//curl -X POST -H 'Authorization: Bearer xoxb-1234-56789abcdefghijklmnop' \
slackToken="xoxb-895731779237-1034747577094-NbWn0GrGVbJLNicrxNFk7PhQ";



// WHEN I search for a city
// THEN I am presented with current and future conditions for that city and that city is added to the search history
$("#searchButton").on("click", function() {

    // make API request
    //api.openweathermap.org/data/2.5/weather?q=London
    Description:
Please, use your API key in each API call.

We do not process API requests without the API key.

API call:
http://api.openweathermap.org/data/2.5/forecast?id=524901&APPID={APIKEY}
Parameters:
APPID {APIKEY} is your unique API key
Example of API call:
  



}

// each save button  on click saves to local storage
$(".saveBtn").on( "click", function() {
    // stops execution of default action
    event.preventDefault();
    // calculate DOM id of corresponding activity note e.g.==> an + 03
    let id = this.id.substr(2);
    // save to local storage, create key-value e.g. an03-value_of(#an03)
    localStorage.setItem("an"+id,$("#an"+id).val());
    // add tick icon to the left column
    $("#cl" + id).addClass("bg-warning");
});



var url = "https://hooks.slack.com/services/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";
var text = "This bs finall works... we will use webhooks...";

$.ajax({
    data: 'payload=' + JSON.stringify({
        "text": text
    }),
    dataType: 'json',
    processData: false,
    type: 'POST',
    url: url
});




})
